This project implements a real-time traffic vehicle counting system using computer vision. The program detects vehicles in a video, tracks each vehicle with a unique ID, and counts how many vehicles cross two virtual lines (Red and Green).

The system uses YOLOv8 object detection for identifying vehicles and OpenCV for video processing and visualization.
